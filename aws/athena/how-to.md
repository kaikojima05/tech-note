## 動作の流れ

Athena は実行したクエリに対して、一度 **QueryExcutionId** を発行して終了する。
<br />実行したクエリはバックエンドで非同期で動作し続け、完了後に発行した **QueryExcutionId** に結果を紐づける。
<br />呼び出し側は Athena から返された **QueryExcutionId** を用いて、結果の問い合わせを行う。
<br />なお、結果の取得完了は呼び出し側に通知が来るわけではない為、問い合わせにはポーリングが必須になる。
